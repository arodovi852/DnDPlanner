/**
 * Landing page (/main).
 * Página de aterrizaje por ahora vacía — se rellenará en iteraciones
 * futuras con el hero, campañas destacadas, etc.
 */
export function MainPage() {
  return (
    <section aria-labelledby="main-heading" style={{ padding: 'var(--space-xl)' }}>
      <h1 id="main-heading">DnDPlanner</h1>
      <p>Bienvenido a DnDPlanner — tu gestor de campañas de rol.</p>
    </section>
  );
}

export default MainPage;
